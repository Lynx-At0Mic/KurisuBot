{ 
    "BOT_CONFIG" : {
        "TOKEN" : "TOKEN GOES HERE. RENAME THIS FILE TO 'config.json'",
        "PREFIX" : "PREFIX HERE"
    },
    "DB_CONFIG" : {
        "USERNAME" : "USERNAME HERE",
        "PASSWORD" : "PASSWORD HERE",
        "DATABASE" : "DATABASE NAME HERE",
        "HOST" : "HOST HERE",

        "TABLES" : {
            "economy": "CREATE TABLE `economy` ( `guildID` VARCHAR(255) NOT NULL , `userID` VARCHAR(255) NOT NULL , `balance` INT NOT NULL, `dailyUsed` INT NULL DEFAULT NULL) ENGINE = InnoDB"
        }
    }
}